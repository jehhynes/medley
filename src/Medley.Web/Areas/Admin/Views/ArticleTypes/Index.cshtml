@model IEnumerable<Medley.Domain.Entities.ArticleType>
@{
    ViewData["Title"] = "Article Types";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="main-content-header d-flex justify-content-between align-items-start mb-4">
    <div>
        <h1 class="main-content-title">Article Types</h1>
        <p class="text-muted">Manage article type categories and their icons.</p>
    </div>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> New Article Type
    </a>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}

<!-- Hidden CSRF token for AJAX requests -->
@Html.AntiForgeryToken()

<div class="card">
    <div class="card-header">
        <h5 class="card-title mb-0">All Article Types</h5>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            @if (Model.Any())
            {
                <table class="table align-middle mb-0">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Icon</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var articleType in Model)
                        {
                            <tr>
                                <td class="fw-semibold">@articleType.Name</td>
                                <td>
                                    @if (!string.IsNullOrWhiteSpace(articleType.Icon))
                                    {
                                        var iconClass = articleType.Icon.StartsWith("bi-") ? $"bi {articleType.Icon}" :
                                                        articleType.Icon.StartsWith("fa-") ? $"fas {articleType.Icon}" :
                                                        articleType.Icon;

                                        <i class="@iconClass"></i>
                                        <span class="text-muted ms-2">@articleType.Icon</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">None</span>
                                    }
                                </td>
                                <td class="text-end text-nowrap">
                                    <a asp-action="Edit" asp-route-id="@articleType.Id" class="btn btn-sm btn-primary me-2">
                                        <i class="bi bi-pencil"></i> Edit
                                    </a>
                                    <div class="btn-group">
                                        <button type="button" class="btn btn-sm btn-outline-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                                            <i class="bi bi-three-dots"></i>
                                        </button>
                                        <ul class="dropdown-menu dropdown-menu-end">
                                            <li>
                                                <button type="button" class="dropdown-item text-danger delete-articletype" data-id="@articleType.Id" data-name="@articleType.Name">
                                                    <i class="bi bi-trash"></i> Delete
                                                </button>
                                            </li>
                                        </ul>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div class="text-center py-5">
                    <i class="bi bi-file-text text-muted" style="font-size: 3rem;"></i>
                    <h4 class="text-muted mt-3">No article types found</h4>
                    <p class="text-muted">Get started by creating your first article type.</p>
                    <a asp-action="Create" class="btn btn-primary">
                        <i class="bi bi-plus-circle"></i> New Article Type
                    </a>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/lib/bootbox/dist/bootbox.all.min.js" asp-append-version="true"></script>
    <script>
        document.querySelectorAll('.delete-articletype').forEach(button => {
            button.addEventListener('click', function() {
                const articleTypeId = this.dataset.id;
                const articleTypeName = this.dataset.name;
                
                bootbox.confirm({
                    title: 'Delete Article Type',
                    message: `Are you sure you want to delete the article type <strong>${articleTypeName}</strong>?`,
                    buttons: {
                        cancel: {
                            label: 'Cancel',
                            className: 'btn-secondary'
                        },
                        confirm: {
                            label: 'Delete',
                            className: 'btn-danger'
                        }
                    },
                    callback: function(result) {
                        if (result) {
                            const form = document.createElement('form');
                            form.method = 'POST';
                            form.action = '@Url.Action("Delete", "ArticleTypes")/' + articleTypeId;
                            
                            const token = document.querySelector('input[name="__RequestVerificationToken"]');
                            if (token) {
                                const tokenInput = document.createElement('input');
                                tokenInput.type = 'hidden';
                                tokenInput.name = '__RequestVerificationToken';
                                tokenInput.value = token.value;
                                form.appendChild(tokenInput);
                            }
                            
                            document.body.appendChild(form);
                            form.submit();
                        }
                    }
                });
            });
        });
    </script>
}

